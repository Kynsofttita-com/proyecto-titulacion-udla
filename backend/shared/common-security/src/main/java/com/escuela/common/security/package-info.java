/**
 * Utilidades compartidas de seguridad para todos los microservicios.
 *
 * <p>Este paquete contendrá (a implementar en Sprint 4):</p>
 * <ul>
 *   <li>{@code JwtTokenProvider} - Generación y validación de tokens JWT</li>
 *   <li>{@code JwtAuthenticationFilter} - Filtro que intercepta requests y valida JWT</li>
 *   <li>{@code SecurityConfig} - Configuración base de Spring Security</li>
 *   <li>{@code JwtProperties} - Configuración tipada (secret, expiraciones)</li>
 * </ul>
 */
package com.escuela.common.security;
