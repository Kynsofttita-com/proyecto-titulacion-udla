services:
  # Base de Datos
  postgresql:
    image: postgres:15-alpine
    container_name: proyecto-postgres
    environment:
      POSTGRES_DB: proyecto_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - proyecto-network

  # Message Broker
  rabbitmq:
    image: rabbitmq:3.12-management-alpine
    container_name: proyecto-rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    ports:
      - "5672:5672"
      - "15672:15672"
    healthcheck:
      test: rabbitmq-diagnostics -q ping
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - proyecto-network

  # Object Storage (S3-compatible)
  minio:
    image: minio/minio:latest
    container_name: proyecto-minio
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY:-minioadmin123}
    ports:
      - "9000:9000"  # API S3
      - "9001:9001"  # Web Console
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks:
      - proyecto-network

  # Service Discovery
  eureka:
    build:
      context: ../../backend
      dockerfile: eureka-server/Dockerfile
    container_name: proyecto-eureka
    environment:
      SPRING_APPLICATION_NAME: eureka-server
      SERVER_PORT: 8761
      JAVA_OPTS: -Duser.timezone=America/Guayaquil
    ports:
      - "8761:8761"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8761/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 180s
    networks:
      - proyecto-network
    depends_on:
      - postgresql
      - rabbitmq

  # API Gateway
  api-gateway:
    build:
      context: ../../backend
      dockerfile: api-gateway/Dockerfile
    container_name: proyecto-gateway
    environment:
      SPRING_APPLICATION_NAME: api-gateway
      SERVER_PORT: 8080
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka:8761/eureka/
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_PORT: 5672
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgresql:5432/proyecto_db
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres123
      JAVA_OPTS: -Duser.timezone=America/Guayaquil
      JWT_SECRET: ${JWT_SECRET:-miSecretaJWT123SecureKey256BitsBitsBits}
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 180s
    networks:
      - proyecto-network
    depends_on:
      eureka:
        condition: service_healthy
      postgresql:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  # MS-Auth
  ms-auth:
    build:
      context: ../../backend
      dockerfile: ms-auth/Dockerfile
    container_name: proyecto-ms-auth
    environment:
      SPRING_APPLICATION_NAME: ms-auth
      SERVER_PORT: 8081
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka:8761/eureka/
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_PORT: 5672
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgresql:5432/proyecto_db
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres123
      JAVA_OPTS: -Duser.timezone=America/Guayaquil
      JWT_SECRET: ${JWT_SECRET:-miSecretaJWT123SecureKey256BitsBitsBits}
    ports:
      - "8081:8081"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/actuator/health"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 180s
    networks:
      - proyecto-network
    depends_on:
      eureka:
        condition: service_healthy
      postgresql:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  # MS-Estudiantes
  ms-estudiantes:
    build:
      context: ../../backend
      dockerfile: ms-estudiantes/Dockerfile
    container_name: proyecto-ms-estudiantes
    environment:
      SPRING_APPLICATION_NAME: ms-estudiantes
      SERVER_PORT: 8082
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka:8761/eureka/
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_PORT: 5672
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgresql:5432/proyecto_db
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres123
      JAVA_OPTS: -Duser.timezone=America/Guayaquil
      JWT_SECRET: ${JWT_SECRET:-miSecretaJWT123SecureKey256BitsBitsBits}
    ports:
      - "8082:8082"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8082/actuator/health"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 180s
    networks:
      - proyecto-network
    depends_on:
      eureka:
        condition: service_healthy
      postgresql:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  # MS-Instructores
  ms-instructores:
    build:
      context: ../../backend
      dockerfile: ms-instructores/Dockerfile
    container_name: proyecto-ms-instructores
    environment:
      SPRING_APPLICATION_NAME: ms-instructores
      SERVER_PORT: 8083
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka:8761/eureka/
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_PORT: 5672
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgresql:5432/proyecto_db
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres123
      JAVA_OPTS: -Duser.timezone=America/Guayaquil
      JWT_SECRET: ${JWT_SECRET:-miSecretaJWT123SecureKey256BitsBitsBits}
    ports:
      - "8083:8083"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8083/actuator/health"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 180s
    networks:
      - proyecto-network
    depends_on:
      eureka:
        condition: service_healthy
      postgresql:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  # MS-Vehículos
  ms-vehiculos:
    build:
      context: ../../backend
      dockerfile: ms-vehiculos/Dockerfile
    container_name: proyecto-ms-vehiculos
    environment:
      SPRING_APPLICATION_NAME: ms-vehiculos
      SERVER_PORT: 8084
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka:8761/eureka/
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_PORT: 5672
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgresql:5432/proyecto_db
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres123
      JAVA_OPTS: -Duser.timezone=America/Guayaquil
      JWT_SECRET: ${JWT_SECRET:-miSecretaJWT123SecureKey256BitsBitsBits}
    ports:
      - "8084:8084"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8084/actuator/health"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 180s
    networks:
      - proyecto-network
    depends_on:
      eureka:
        condition: service_healthy
      postgresql:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  # MS-Asignaciones
  ms-asignaciones:
    build:
      context: ../../backend
      dockerfile: ms-asignaciones/Dockerfile
    container_name: proyecto-ms-asignaciones
    environment:
      SPRING_APPLICATION_NAME: ms-asignaciones
      SERVER_PORT: 8085
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka:8761/eureka/
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_PORT: 5672
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgresql:5432/proyecto_db
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres123
      JAVA_OPTS: -Duser.timezone=America/Guayaquil
      JWT_SECRET: ${JWT_SECRET:-miSecretaJWT123SecureKey256BitsBitsBits}
      FEIGN_MS_ESTUDIANTES_URL: http://ms-estudiantes:8082
      FEIGN_MS_INSTRUCTORES_URL: http://ms-instructores:8083
      FEIGN_MS_VEHICULOS_URL: http://ms-vehiculos:8084
    ports:
      - "8085:8085"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8085/actuator/health"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 180s
    networks:
      - proyecto-network
    depends_on:
      eureka:
        condition: service_healthy
      postgresql:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  # MS-Cobros
  ms-cobros:
    build:
      context: ../../backend
      dockerfile: ms-cobros/Dockerfile
    container_name: proyecto-ms-cobros
    environment:
      SPRING_APPLICATION_NAME: ms-cobros
      SERVER_PORT: 8086
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka:8761/eureka/
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_PORT: 5672
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgresql:5432/proyecto_db
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres123
      JAVA_OPTS: -Duser.timezone=America/Guayaquil
      JWT_SECRET: ${JWT_SECRET:-miSecretaJWT123SecureKey256BitsBitsBits}
      FEIGN_MS_ESTUDIANTES_URL: http://ms-estudiantes:8082
      FEIGN_MS_AUTH_URL: http://ms-auth:8081
    ports:
      - "8086:8086"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8086/actuator/health"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 180s
    networks:
      - proyecto-network
    depends_on:
      eureka:
        condition: service_healthy
      postgresql:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  # MS-Reportes
  ms-reportes:
    build:
      context: ../../backend
      dockerfile: ms-reportes/Dockerfile
    container_name: proyecto-ms-reportes
    environment:
      SPRING_APPLICATION_NAME: ms-reportes
      SERVER_PORT: 8087
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka:8761/eureka/
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_PORT: 5672
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgresql:5432/proyecto_db
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres123
      JAVA_OPTS: -Duser.timezone=America/Guayaquil
      JWT_SECRET: ${JWT_SECRET:-miSecretaJWT123SecureKey256BitsBitsBits}
    ports:
      - "8087:8087"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8087/actuator/health"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 180s
    networks:
      - proyecto-network
    depends_on:
      eureka:
        condition: service_healthy
      postgresql:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  # MS-Notificaciones
  ms-notificaciones:
    build:
      context: ../../backend
      dockerfile: ms-notificaciones/Dockerfile
    container_name: proyecto-ms-notificaciones
    environment:
      SPRING_APPLICATION_NAME: ms-notificaciones
      SERVER_PORT: 8088
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka:8761/eureka/
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_PORT: 5672
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgresql:5432/proyecto_db
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres123
      JAVA_OPTS: -Duser.timezone=America/Guayaquil
      JWT_SECRET: ${JWT_SECRET:-miSecretaJWT123SecureKey256BitsBitsBits}
    ports:
      - "8088:8088"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8088/actuator/health"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 180s
    networks:
      - proyecto-network
    depends_on:
      eureka:
        condition: service_healthy
      postgresql:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  # Frontend (Vue.js)
  frontend:
    build:
      context: ../../frontend
      dockerfile: Dockerfile
    container_name: proyecto-frontend
    environment:
      VITE_API_URL: http://localhost:8080
    ports:
      - "3000:80"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s
    networks:
      - proyecto-network
    depends_on:
      api-gateway:
        condition: service_healthy

  # Adminer - Admin BD
  adminer:
    image: adminer:latest
    container_name: proyecto-adminer
    ports:
      - "8089:8080"
    environment:
      ADMINER_DEFAULT_SERVER: postgresql
    networks:
      - proyecto-network
    depends_on:
      - postgresql

  # Jenkins - CI/CD
  jenkins:
    build:
      context: ../../.deployment/jenkins
      dockerfile: Dockerfile
    container_name: proyecto-jenkins
    environment:
      JAVA_OPTS: -Duser.timezone=America/Guayaquil
      JENKINS_OPTS: --httpPort=8090
    ports:
      - "8090:8090"
      - "50000:50000"
    volumes:
      - jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - proyecto-network
    depends_on:
      - postgresql
      - rabbitmq

volumes:
  jenkins_home:
  postgres_data:
  minio_data:

networks:
  proyecto-network:
    driver: bridge
