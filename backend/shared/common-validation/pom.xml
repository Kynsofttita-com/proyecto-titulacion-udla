<?xml version="1.0" encoding="UTF-8"?>
<!--
    common-validation
    =================
    Libreria compartida con validadores de datos especificos de Ecuador.

    Provee:
      - Validadores estaticos (core/): algoritmos puros sin dependencias.
        - CedulaEcuadorValidator   : algoritmo modulo 10 (digito verificador).
        - RucEcuadorValidator      : 13 digitos (10 cedula valida + 001).
        - PlacaEcuadorValidator    : regex ABC-1234 o AB-1234A.
        - TelefonoEcuadorValidator : movil 09XXXXXXXX o fijo Quito 02XXXXXXX.
      - Custom annotations (annotation/): @CedulaEcuador, @RucEcuador,
        @PlacaEcuador, @TelefonoEcuador para usar como bean validation en DTOs.
      - ConstraintValidator (constraint/): puentes JSR-380 entre annotations
        y validadores core.

    Decisiones (DECISIONES.md seccion 11):
      - Validacion en backend con custom validators + Bean Validation.
      - Reutilizable por todos los MS del Grupo A (Auth, Estudiantes,
        Instructores, Vehiculos, Asignaciones, Cobros).
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.escuela</groupId>
        <artifactId>escuela-backend-parent</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <artifactId>common-validation</artifactId>
    <packaging>jar</packaging>

    <name>Common Validation</name>
    <description>Validadores compartidos de datos Ecuador (cedula, RUC, placa, telefono)</description>

    <dependencies>
        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- SLF4J para logging -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>

        <!-- Bean Validation (JSR 380) - aporta @Constraint, ConstraintValidator -->
        <dependency>
            <groupId>jakarta.validation</groupId>
            <artifactId>jakarta.validation-api</artifactId>
        </dependency>

        <!-- Tests -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>${project.artifactId}</finalName>
    </build>
</project>
