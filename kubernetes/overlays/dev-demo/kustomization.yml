apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../../base

namespace: escuela

namePrefix: demo-

commonLabels:
  environment: demo
  team: tita-group

replicas:
  - name: api-gateway
    count: 1
  - name: eureka-server
    count: 1

resources:
  - namespace-patch.yml
