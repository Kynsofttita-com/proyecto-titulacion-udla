apiVersion: v1
kind: Service
metadata:
  name: ms-asignaciones
  namespace: escuela-conduccion
spec:
  ports:
    - port: 8085
      targetPort: 8085
  selector:
    app: ms-asignaciones
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-asignaciones
  namespace: escuela-conduccion
spec:
  replicas: 1
  selector: { matchLabels: { app: ms-asignaciones } }
  template:
    metadata:
      labels: { app: ms-asignaciones }
    spec:
      containers:
        - name: ms-asignaciones
          image: ghcr.io/kynsofttita-com/proyecto-titulacion-udla/ms-asignaciones:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8085
          envFrom:
            - configMapRef: { name: app-config }
          env:
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom: { secretKeyRef: { name: db-secrets, key: SPRING_DATASOURCE_USERNAME } }
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom: { secretKeyRef: { name: db-secrets, key: SPRING_DATASOURCE_PASSWORD } }
            - name: SPRING_RABBITMQ_USERNAME
              valueFrom: { secretKeyRef: { name: rabbitmq-secrets, key: SPRING_RABBITMQ_USERNAME } }
            - name: SPRING_RABBITMQ_PASSWORD
              valueFrom: { secretKeyRef: { name: rabbitmq-secrets, key: SPRING_RABBITMQ_PASSWORD } }
            - name: JWT_SECRET
              valueFrom: { secretKeyRef: { name: app-secrets, key: JWT_SECRET } }
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8085 }
            initialDelaySeconds: 60
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 8085 }
            initialDelaySeconds: 180
            periodSeconds: 30
          resources:
            requests: { cpu: "150m", memory: "512Mi" }
            limits:   { cpu: "1",    memory: "1Gi" }
