<?xml version="1.0" encoding="UTF-8"?>
<!--
    POM padre del backend - Sistema de Control Administrativo para Escuelas de Conducción
    =====================================================================================
    Centraliza la gestión de versiones, propiedades y plugins para todos los microservicios.
    Hereda de spring-boot-starter-parent para integración estándar de Spring Boot 3.

    Stack: Java 21 + Spring Boot 3.4 + Spring Cloud 2024.0
    Ver DECISIONES.md para detalles de cada dependencia.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- Heredamos de Spring Boot Starter Parent para gestión automática de dependencias -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.0</version>
        <relativePath/>
    </parent>

    <!-- Coordenadas del POM padre -->
    <groupId>com.escuela</groupId>
    <artifactId>escuela-backend-parent</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>Escuela de Conducción - Backend Parent</name>
    <description>POM padre que agrupa todos los microservicios del sistema</description>

    <!-- Módulos hijos (microservicios + soporte + librerías compartidas) -->
    <modules>
        <!-- Librerías compartidas -->
        <module>shared/common-events</module>
        <module>shared/common-exceptions</module>
        <module>shared/common-security</module>
        <module>shared/common-jpa</module>
        <module>shared/common-validation</module>

        <!-- Servicios de soporte -->
        <module>eureka-server</module>
        <module>api-gateway</module>

        <!-- Microservicios de dominio -->
        <module>ms-auth</module>
        <module>ms-estudiantes</module>
        <module>ms-instructores</module>
        <module>ms-vehiculos</module>
        <module>ms-asignaciones</module>
        <module>ms-cobros</module>
        <module>ms-reportes</module>
        <module>ms-notificaciones</module>
    </modules>

    <!-- Propiedades centralizadas -->
    <properties>
        <!-- Java -->
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!-- Versiones de frameworks principales -->
        <spring-cloud.version>2024.0.0</spring-cloud.version>

        <!-- Versiones de librerías de terceros -->
        <mapstruct.version>1.6.3</mapstruct.version>
        <lombok.version>1.18.36</lombok.version>
        <lombok-mapstruct-binding.version>0.2.0</lombok-mapstruct-binding.version>
        <jjwt.version>0.12.6</jjwt.version>
        <springdoc.version>2.7.0</springdoc.version>
        <logstash-logback.version>8.0</logstash-logback.version>
        <resilience4j.version>2.2.0</resilience4j.version>
        <minio.version>8.5.14</minio.version>
        <flyway.version>10.20.1</flyway.version>
        <testcontainers.version>1.20.4</testcontainers.version>
        <mockito.version>5.14.2</mockito.version>

        <!-- Plugins -->
        <jacoco.version>0.8.12</jacoco.version>
        <surefire.version>3.5.2</surefire.version>

        <!-- Cobertura mínima (DECISIONES.md) -->
        <jacoco.coverage.minimum>0.80</jacoco.coverage.minimum>
    </properties>

    <!-- Gestión centralizada de dependencias (versiones) -->
    <dependencyManagement>
        <dependencies>
            <!-- Spring Cloud BOM -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Testcontainers BOM -->
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Resilience4j BOM (circuit breakers) -->
            <dependency>
                <groupId>io.github.resilience4j</groupId>
                <artifactId>resilience4j-bom</artifactId>
                <version>${resilience4j.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- ===== Módulos compartidos del proyecto ===== -->
            <dependency>
                <groupId>com.escuela</groupId>
                <artifactId>common-events</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.escuela</groupId>
                <artifactId>common-exceptions</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.escuela</groupId>
                <artifactId>common-security</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.escuela</groupId>
                <artifactId>common-jpa</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.escuela</groupId>
                <artifactId>common-validation</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- ===== MapStruct (mapeo Entity <-> DTO) ===== -->
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct-processor</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>

            <!-- ===== JWT (JSON Web Tokens) ===== -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>

            <!-- ===== OpenAPI / Swagger UI ===== -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>

            <!-- ===== Logging estructurado JSON ===== -->
            <dependency>
                <groupId>net.logstash.logback</groupId>
                <artifactId>logstash-logback-encoder</artifactId>
                <version>${logstash-logback.version}</version>
            </dependency>

            <!-- ===== MinIO (object storage) ===== -->
            <dependency>
                <groupId>io.minio</groupId>
                <artifactId>minio</artifactId>
                <version>${minio.version}</version>
            </dependency>

            <!-- ===== Resilience4j OpenFeign Bridge ===== -->
            <dependency>
                <groupId>io.github.resilience4j</groupId>
                <artifactId>resilience4j-feign</artifactId>
            </dependency>

            <!-- ===== HTTP Client 5 (para Feign circuit breaker) ===== -->
            <dependency>
                <groupId>org.apache.httpcomponents.client5</groupId>
                <artifactId>httpclient5</artifactId>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- Configuración de plugins de build -->
    <build>
        <!-- JaCoCo plugin execution (must run on all builds for coverage reports) -->
        <plugins>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report-aggregate</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report-aggregate</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>

        <pluginManagement>
            <plugins>
                <!-- Compilador con annotation processors (Lombok + MapStruct) -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                            <path>
                                <groupId>org.mapstruct</groupId>
                                <artifactId>mapstruct-processor</artifactId>
                                <version>${mapstruct.version}</version>
                            </path>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok-mapstruct-binding</artifactId>
                                <version>${lombok-mapstruct-binding.version}</version>
                            </path>
                        </annotationProcessorPaths>
                        <compilerArgs>
                            <arg>-Amapstruct.defaultComponentModel=spring</arg>
                            <arg>-Amapstruct.unmappedTargetPolicy=ERROR</arg>
                            <arg>-parameters</arg>
                        </compilerArgs>
                    </configuration>
                </plugin>

                <!-- Surefire (tests unitarios) -->
                <!--
                    Nota: NO usamos <excludedGroups> a nivel padre porque requiere que
                    junit-jupiter-engine esté en classpath, lo cual no ocurre en modules
                    shared/* (no tienen tests). Los integration tests se separan por
                    naming convention: *Tests.java (unit) vs *IT.java (integration via
                    failsafe). failIfNoTests=false evita errores en modules sin tests.

                    Java 21: Mockito 5.14.2+ auto-detects si puede self-attach.
                    El warning "self-attaching" es informativo, no es bloqueante.
                    Se recomienda mantener así en lugar de usar javaagent explícito.
                -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${surefire.version}</version>
                    <configuration>
                        <failIfNoTests>false</failIfNoTests>
                        <argLine>-Duser.timezone=America/Guayaquil</argLine>
                    </configuration>
                </plugin>

                <!-- Failsafe (tests de integración - naming convention *IT.java) -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <version>${surefire.version}</version>
                    <configuration>
                        <failIfNoTests>false</failIfNoTests>
                    </configuration>
                </plugin>

                <!-- Spring Boot Maven Plugin (build de jar ejecutable) -->
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <!-- Repositorios (Spring Snapshots/Milestones si fueran necesarios en el futuro) -->
    <repositories>
        <repository>
            <id>maven-central</id>
            <name>Maven Central</name>
            <url>https://repo.maven.apache.org/maven2</url>
        </repository>
    </repositories>
</project>
