/**
 * Modulo principal de la aplicacion
 * Contiene funciones basicas para demostrar el pipeline CI/CD
 */

function sumar(a, b) {
  return a + b;
}

function restar(a, b) {
  return a - b;
}

function multiplicar(a, b) {
  return a * b;
}

module.exports = {
  sumar,
  restar,
  multiplicar
};
